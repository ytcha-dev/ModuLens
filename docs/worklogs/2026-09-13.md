# 2026-09-13 Worklog

## 02:36 — PRD 初次審閱

- 使用者需求摘要：審閱新加入的 `PRD.md`，並針對內容給予回饋；本次不要求實作 PRD 或直接修改 PRD。
- 解讀、假設與設計決策：以需求完整性、領域契約與失敗模式、可驗收性、V1 範圍一致性為審閱主軸。由於目前工作目錄缺少 `Goal.txt`、`docs/AgentLab-Development-Plan.md`、主要 userscript fixture 與 Git metadata，僅對 PRD 本身做完整審閱，不推測不存在的 roadmap 或既有實作。
- 實際動作與變更檔案：完整閱讀 `AGENTS.md` 與 `PRD.md`；確認工作目錄內容與 Git 狀態；新增本 worklog。未修改 `PRD.md`。
- 審閱結論摘要：產品問題、V1/V2 邊界、incremental milestones 與 Core/UI 分離方向清楚；開始 Milestone 1–2 前，建議優先補齊 `SourceRange` 的精確語意、文字／位元保存契約、section heading grammar、重名 section 身份與 CLI 成功／錯誤契約。後續 V1 尚需定義外部檔案變更衝突、Git repository/baseline 邊界，以及 dependency graph 的單一最小資料來源。
- 測試與驗證：以檔案清單、全文閱讀及行號定位進行文件一致性檢查；`git status --short` 回傳「not a git repository」，因此無法執行 diff 或 commit。此階段為文件審閱，沒有可執行程式、build 或 unit test 可跑。
- 未完成／受阻與建議下一步：`Goal.txt` 與 `docs/AgentLab-Development-Plan.md` 不存在，無法核對 PRD 與既有 goal/roadmap；主要 userscript fixture 不存在，無法確認 heading 範式與預期 section 清單；目前也沒有 Git repository，無法依協作規範提交本 worklog。建議先修訂 Milestone 1–2 契約，再補入 fixture 與 expected-section manifest，之後才開始 scaffold。
- 對使用者的最終回饋摘要：提供依優先級排序的 PRD findings、保留項目與可直接補入 PRD 的驗收條款建議，並清楚揭露缺少基準文件與 Git 狀態的限制。

### 使用者操作指南（當前版本）

本階段未新增可操作功能；僅完成 PRD 文件審閱與審閱紀錄。

## 02:43 — 依回饋精簡修訂 PRD

- 使用者需求摘要：確認先前建議是否過度處理；說明 `sample.js` 不應成為唯一輸入標準；若方向可行，直接改進 PRD。
- 解讀、假設與設計決策：採最小必要契約，不把 encoding、atomic save、外部編輯衝突或完整 Git edge cases提前納入 Milestone 1–2。`sample.js` 定位為代表性 integration fixture；小型 synthetic fixtures 才是 parser grammar 與 edge cases 的權威規格。
- 實際動作與變更檔案：檢查 `sample.js` 的實際 heading；將 `PRD.md` 版本更新為 0.2；補充 V1 module 與 `SourceSection` 的關係、初始 JSDoc heading grammar、range 與文字保存契約、duplicate section identity、雙層 fixture 策略及最小 CLI 輸出／失敗契約。
- 重要發現：`sample.js` 共有 2,774 行與 21 個 section；其中 `RIGHT CLICK` heading 在名稱與結尾 separator 之間包含說明文字，原本限定的五行 heading 形式會漏掉它。修訂後 grammar 明確容許這種描述行，但仍不擴張到其他 comment style、annotation 或 AI inference。
- 測試與驗證：以 PowerShell 依 PRD 0.2 grammar 逐行 smoke check `sample.js`，結果為 PASS：2,774 行、21 個 section、`Video detection` range 為 212–379、成功辨識帶描述的 `RIGHT CLICK`；並確認 PRD Markdown code fences 成對。此階段仍未建立程式碼，沒有 build 或 unit test 可執行。
- 未完成／受阻與建議下一步：`Goal.txt`、`docs/AgentLab-Development-Plan.md` 與 Git repository 仍不存在，因此無法做 roadmap 一致性核對或建立文件 milestone commit。下一步可依 PRD 0.2 開始 Milestones 1–2，但不自動實作。
- 對使用者的最終回饋摘要：說明已把建議收斂至會影響首階段一致性的最小契約，並讓 sample 保持「真實驗證案例」而不是「唯一格式來源」。

### 使用者操作指南（當前版本）

本階段未新增可操作功能；PRD 0.2 僅建立下一階段的實作與驗收契約。

## 02:55 — 開始 Milestones 1–2 實作

- 使用者需求摘要：接受 PRD 0.2 並開始 Milestones 1–2；明確區分 `FullRange`、`HeaderRange`、`ContentRange`，加入 XML docs 與兩層測試；完成 Core model、窄範圍 parser、CLI 後停止，不進入 Git、GUI、AST、編輯與儲存功能。
- 解讀、假設與設計決策：字元 offset 採零基底半開區間 `[StartOffset, EndOffset)`；非空 range 的 public 行號採 1-based inclusive；空 `ContentRange` 以相同 offsets、`IsEmpty == true`、`EndLine == StartLine - 1` 表達。`HeaderRange` 包含完整 heading JSDoc 及 closing delimiter 的換行（若存在）；`ContentRange` 保留 header 後到下一 heading 前的所有原始字元，包括格式空白。
- 實際動作與變更檔案：將 range 契約與 `Video detection`／`RIGHT CLICK` 例子補入 `PRD.md`；建立 .NET 10 solution、Core、CLI 與 xUnit 專案；實作 `SourceRange`、`SourceSection`、`SourceDocument`、`SectionParser`、CLI；新增 synthetic unit tests 與 `sample.js` integration test。
- 測試與驗證：`dotnet test .\ModuLens.sln --configuration Release --no-restore` 通過 13/13 tests；`dotnet build .\ModuLens.sln --configuration Release --no-restore` 成功且為 0 warnings/0 errors；`dotnet format .\ModuLens.sln --verify-no-changes --no-restore` 通過。CLI 對 `sample.js` exit code 0 並輸出 21 個 sections；直接執行 CLI 時，缺少參數回傳 2、檔案不存在回傳 3。首次 restore 因 sandbox 內 NuGet TLS 存取失敗，經核准後完成套件還原，後續驗證皆使用 `--no-restore`。
- 未完成／受阻與建議下一步：目前 `Goal.txt`、`docs/AgentLab-Development-Plan.md` 與 Git repository 仍不存在，因此無法核對 roadmap 或建立規範要求的 milestone commit。功能依要求停在 Milestones 1–2；Git、GUI、AST、Editable Modules、encoding/save strategy 與 generic JavaScript parsing 均未開始。
- 對使用者的最終回饋摘要：Milestones 1–2 已完成並驗證；回報建立與修改檔案、domain/range 契約、parser 演算法、13 項測試、sample integration 結果及已知限制。

### 使用者操作指南（當前版本）

- 功能範圍：Milestones 1–2，載入單一 JavaScript 檔並列出偵測到的 section 名稱與 `FullRange` 起訖行。
- 前置條件：.NET 10 SDK；不需 API key、credential、Git repository 或外部服務。
- 操作：`dotnet run --project .\src\ModuLens.Cli\ModuLens.Cli.csproj -- .\sample.js`。
- 預期輸出：tab-separated header `Name StartLine EndLine`，接著依原始碼順序列出 21 個 sample sections。
- 成功確認：process exit code 為 0，輸出包含 `Video detection 212 379`、`RIGHT CLICK 2079 2230` 與 `Start 2757 2774`。
- 當前限制：只支援 PRD 0.2 的 structured JSDoc heading grammar；不含 Git、GUI、AST、編輯、encoding/save strategy 或 generic JavaScript parsing。

## 03:19 — Goal 審閱與初始 Git 提交規劃

- 使用者需求摘要：審閱新加入的 `Goal.txt` 是否合理；Git 已初始化且尚無 commit；若可行，將初期規劃與已完成的 Milestones 1–2 實作拆成不同 commits。
- 解讀、假設與設計決策：Goal 的長期順序合理，但 current/next 狀態已落後，仍把完成的 Source Model 與 Section Parser 列為下一步；將它更新為已完成，並把 Read-only Module Explorer 標示為下一個未開始 milestone。因 `AGENTS.md` 指定的 development plan 缺失，新增一份只整合 PRD 與 Goal、未擴張產品範圍的精簡計畫。`sample.js` 是規劃期選定的驗證基準，歸入初期規劃 commit；solution、Core、CLI 與 tests 歸入實作 commit。空的 `.vscode/settings.json` 與本次需求無關，保留為 untracked，不納入提交。
- 實際動作與變更檔案：審閱並更新 `Goal.txt`；新增 `docs/AgentLab-Development-Plan.md`；建立規劃 commit `c76684d`（`docs: establish initial ModuLens plan`），只納入規劃、規範、worklog、`.gitignore` 與 `sample.js`；建立實作 commit `c0fa10c`（`feat(core): implement source section parsing`），納入 solution、Core、CLI、tests 與驗證紀錄。空的 `.vscode/settings.json` 未納入這兩個 commits。
- 測試與驗證：第一個 commit 前完成文件結構、Goal current status、staged file list 與 `git diff --cached --check` 驗證。第二個 commit 前重新執行 `dotnet test`（13/13 passed）、Release build（0 warnings/0 errors）、`dotnet format --verify-no-changes`（passed）與 `sample.js` CLI smoke test（21 sections，代表性 ranges 正確）。
- 未完成／受阻與建議下一步：兩個 commits 已完成；Milestone 3 不在本次授權範圍內，未開始。
- 對使用者的最終回饋摘要：已回報 Goal 審閱結論、兩個 commit hashes、各 commit 檔案範圍、驗證結果及當時刻意保留的 untracked 檔案。

### 使用者操作指南（當前版本）

- 適用範圍：Milestones 1–2；操作方式與限制同上一節。
- 本次變更只校正 roadmap/current status 並整理 Git history，沒有新增 runtime 行為。

## 03:26 — 診斷受管制 Windows 環境的 CLI 啟動失敗

- 使用者需求摘要：使用者在 PowerShell 7.6.6 執行文件中的 `dotnet run` 指令時，Windows 應用程式控制原則封鎖 `bin\Debug\net10.0\ModuLens.Cli.exe`，CLI 無法啟動。
- 解讀、假設與設計決策：錯誤發生在 parser 執行前。曾以 `UseAppHost=false` 驗證是否只有原生 apphost 被封鎖，但政策接著拒絕載入新建的 `ModuLens.Core.dll`，證明不是 project-level apphost 設定可以完整修正，因此已撤回該實驗性變更。不得透過關閉或繞過安全政策解決。
- 實際動作與變更檔案：讀取 Windows Code Integrity Operational events；確認 enforced policy ID `{0283ac0f-fff1-49ae-ada1-8a933130cad6}` 先後封鎖 Debug `ModuLens.Cli.exe` 與 `ModuLens.Core.dll`，理由為未達 Enterprise signing level。僅更新本 worklog，runtime/project files 最終沒有變更。
- 測試與驗證：預設 Debug `dotnet run` 可重現失敗；事件 3033/3077 與 3089 signature events 證實為 App Control enforcement。既有 Release artifact 可用 `dotnet .\src\ModuLens.Cli\bin\Release\net10.0\ModuLens.Cli.dll .\sample.js` 成功輸出 21 sections；13/13 Release tests 亦可執行。曾在 `UseAppHost=false` 實驗期間驗證 Release `dotnet run --no-build`，但撤回設定後該命令會尋找不存在的 apphost，故不列為目前操作方式。這只證明目前既有 Release DLL 已被允許，不保證重新編譯後的新 hash 會獲准。
- 未完成／受阻與建議下一步：若要持續本機開發與執行新 build，需要政策管理者以正式方式允許開發產物，例如組織信任的 code-signing certificate、managed installer 或適當的 supplemental App Control rule。使用者可提供上述 Policy ID 與 3033/3077 events 給管理者。Microsoft 也建議以 App Control event 建立允許規則並先在 audit mode 測試政策。
- 對使用者的最終回饋摘要：提供目前可執行的既有 Release DLL 命令，同時明確說明這不是永久修正；長期解法位於 Windows App Control policy，而非 parser 或 CLI 程式碼。

### 使用者操作指南（當前版本）

- 暫時可用操作：`dotnet .\src\ModuLens.Cli\bin\Release\net10.0\ModuLens.Cli.dll .\sample.js`。
- 預期輸出：依序列出 21 個 sections。
- 限制：上述命令依賴目前已獲准的 Release artifacts；重新編譯或修改後的 DLL 可能再次被政策封鎖。長期需由政策管理者核准開發／簽章流程。
- 後續狀態：使用者確認現階段已可執行，並要求停止延伸診斷、直接總結。由於未另行指定成功使用的命令，本紀錄保留已觀察到的 Code Integrity 事件與已驗證命令，不推測政策狀態已永久改變。

## 03:44 — Milestone 3 Read-only Module Explorer

- 使用者需求摘要：確認 CLI 輸出合理並授權接續下一階段；依 roadmap 執行 Milestone 3。
- 解讀、假設與設計決策：以 .NET 10 WPF 建立初始 Windows desktop shell，不引入第三方 UI 套件。`ModuLens.App` 負責 file dialog 與 filesystem read；Core 保持 UI/filesystem-neutral。Module source view 顯示 `ContentRange`，並另外呈現 content/full line ranges。技術選型記錄於 `docs/decisions/0001-use-wpf-for-initial-desktop-shell.md`。
- 實際動作與變更檔案：新增 `src/ModuLens.App`、read-only `MainWindow` 與 `MainWindowViewModel`；新增 `tests/ModuLens.App.Tests`；將 App 與 tests 加入 `ModuLens.slnx`；更新 `PRD.md`、`Goal.txt`、development plan 與本 worklog。
- 測試與驗證：`dotnet format .\ModuLens.slnx --verify-no-changes --no-restore` 通過；Release build 成功且為 0 warnings/0 errors；Core 13 tests 與 App 5 tests（合計 18/18）通過。App tests 涵蓋 load/empty state、module selection、`sample.js` 的 21 modules／`Transform` content，以及 STA thread 上的實際 MainWindow XAML initialization。
- 未完成／受阻與建議下一步：Milestone 3 功能、驗證與本 worklog 一併作為獨立 stage 提交。Editable Modules、save/reparse、Git、diff 與 graph 均未開始；下一階段需先定義編輯與外部檔案衝突契約。
- 對使用者的最終回饋摘要：Milestone 3 已完成；回報啟動方式、操作流程、UI/domain 邊界、18 項測試、commit 與已明確延後的功能。

### 使用者操作指南（當前版本）

- 適用範圍：Milestone 3 read-only module explorer。
- 前置條件：Windows 與 .NET 10 SDK；不需 API key、credential、Git repository 或外部服務。
- 啟動：`dotnet run --project .\src\ModuLens.App\ModuLens.App.csproj`。
- 操作：點擊 `Open JavaScript…`，選擇 `sample.js`；左側應出現 21 個 modules。點選 `Transform` 後，右側顯示其 content source，標頭顯示 content/full line ranges。
- 成功確認：`Transform` source view 含 `function applyTransform`，不含 `* Transform` heading；原始 `sample.js` 未被修改。
- 當前限制：Windows-only WPF、read-only、一次一個檔案；不含編輯、save/reparse、Git、diff、relationships 或 generic JavaScript parsing。若 Windows App Control 封鎖新建 artifact，需依現行組織安全政策核准，應用程式不會繞過政策。

## 12:17 — Milestone 3 驗收與 module 判定規則說明

- 使用者需求摘要：使用者實際驗證左側 module 清單與右側相關程式碼顯示，認為目前可接受；詢問現行實作如何區分 module，UI 優化延後再議。
- 解讀、假設與設計決策：本次只說明既有契約，不修改 parser 或 UI。V1 的 logical module 與一個 `SourceSection` 一對一；它是由窄範圍 structured JSDoc heading grammar 決定的文字區段，不是依 JavaScript AST、函式、依賴關係或 AI 語意推論分類。
- 實際動作與變更檔案：核對 `SectionParser` 的 heading detection／section building、`MainWindowViewModel` 的選取顯示行為及 PRD 的 V1 module 定義；runtime 檔案未變更，僅更新本 worklog。
- 判定規則摘要：parser 逐行尋找以 `/**` 開始、第一個非空 comment content 為至少三個 `=` 的 separator、下一個非空 content 為 section 名稱，且 `*/` 前最後一個非空 content 也是 separator 的 heading。名稱與結尾 separator 間可有描述文字。每個合法 heading 起始一個 module，範圍延伸至下一個合法 heading 前，最後一個則到 EOF；不符合 grammar 的 comment 不會建立 module。
- 測試與驗證：本次沒有 runtime 變更，因此未重跑 build/test；以既有實作、PRD 與 Milestone 3 的 18/18 passing tests 交叉核對說明。使用者已人工確認 module 清單與 source view 的現行輸出可接受。
- 未完成／受阻與建議下一步：同名 headings 目前仍是依原始順序存在的不同 sections；無合法 heading 的內容不會被推論為 module。若未來 AI 產物不遵循 grammar，需要在後續 milestone 另行設計 AST／語意 inference 與可解釋的 fallback，本次不提前擴張。
- 對使用者的最終回饋摘要：說明目前 module 等同合法 heading 所界定的 `SourceSection`、精確 heading 條件、邊界與 UI 顯示 `ContentRange` 的方式，並區分現行 deterministic parser 與未來可能的語意分析。

### 使用者操作指南（當前版本）

本階段未新增可操作功能；沿用 Milestone 3 的操作方式。左側每一項對應一個符合目前 heading grammar 的 `SourceSection`，右側顯示該 section 的 `ContentRange`，不包含 heading 本身。

## 12:23 — 確認現行 module 限制的後續規劃

- 使用者需求摘要：確認目前 marker-based module 判定的已知限制是否已安排在後續階段處理。
- 解讀、假設與設計決策：區分 roadmap 中不同層級，避免把「後續有規劃」誤解為「下一個 milestone 全部完成」。Milestone 4–7 仍沿用現有 `SourceSection` 邊界完成單檔編輯、Git status／diff 與基礎關係圖；duplicate／malformed／save safety 等可靠性問題屬 V1.1 hardening；沒有 heading 的程式碼、AST／symbol 分析及 automatic module inference 屬 V2。
- 實際動作與變更檔案：核對 `Goal.txt`、`docs/AgentLab-Development-Plan.md` 與 `PRD.md` 的 V1、V1.1、V2 範圍；未修改產品程式，只更新本 worklog。
- 測試與驗證：本次為 roadmap 一致性確認，未變更 runtime，因此不需重跑 build/test；已確認三份規劃文件都將 Automatic Module Inference 安排在 repository model、indexing 與 cross-file AST analysis 之後。
- 未完成／受阻與建議下一步：下一個階段仍是 Milestone 4 Editable Modules，不會在該階段同時加入 AST 或自動推論。未來每一階段開始前仍需依當時需求定義契約並取得使用者授權。
- 對使用者的最終回饋摘要：確認已知問題大致都有對應後續階段，但精確說明哪些屬 V1、V1.1 與 V2，並避免承諾尚未實作的能力。

### 使用者操作指南（當前版本）

本階段未新增可操作功能；目前仍為 Milestone 3 read-only、structured-heading-based module explorer。

## 12:33 — Milestone 4 Editable Modules

- 使用者需求摘要：使用者確認接續 roadmap 的下一階段，實作 Milestone 4 Editable Modules。
- 解讀、假設與設計決策：編輯器只修改所選 `ContentRange`，heading 維持結構性邊界；編輯時使用 transient buffer，切換 module 或儲存前替換完整 source text 中的精確 range，再重新 parse 成新的 immutable `SourceDocument`。Core 不接觸 filesystem；WPF App adapter 負責 encoding-aware save、byte hash 外部修改檢查及同目錄暫存檔替換。詳細決策記錄於 `docs/decisions/0002-use-immutable-edit-snapshots-and-checked-save.md`。
- 實際動作與變更檔案：新增 Core `SourceDocumentEditor`；將 `MainWindowViewModel` 擴充為 editable buffer、dirty state、切換時 commit/reparse、save preparation；WPF 加入可編輯 source view、Save 按鈕、開啟／關閉時未儲存確認；新增 `SourceFileStore`、`SourceFileSnapshot`、`SourceFileChangedException`；補充 Core、ViewModel、storage、XAML 與 `sample.js` integration tests；同步 `PRD.md`、`Goal.txt`、development plan 與 ADR。
- 儲存契約：無 BOM 檔案以 strict UTF-8 處理；保留 UTF-8、UTF-16 LE/BE、UTF-32 LE/BE BOM；儲存前以 SHA-256 比對目前 bytes 與載入 snapshot，不一致即拒絕覆寫並保留 App 內 edits；成功路徑先在來源同目錄寫暫存檔，再替換來源檔。
- 測試與驗證：`dotnet test .\ModuLens.slnx --configuration Release --no-restore` 通過 28/28（Core 16、App 12）；`dotnet format .\ModuLens.slnx --verify-no-changes --no-restore` 通過；Release build 成功且 0 warnings/0 errors；Release CLI smoke test exit code 0、輸出 header 加 21 sections。`sample.js` integration test 在臨時副本修改 `Transform`、儲存、重載後仍辨識 21 sections，驗證 module 外文字未變且 repository fixture bytes 未被修改。
- 未完成／受阻與建議下一步：Milestone 4 不含 Git、module diff、relationship graph、跨檔案或 AST。外部修改目前只拒絕覆寫，未提供 merge；不支援 legacy code pages、backup history 或更強 filesystem durability guarantee。下一個未開始階段為 Milestone 5 Git Module Status。
- 對使用者的最終回饋摘要：Milestone 4 已完成並以獨立 commit 提交；回報可編輯／儲存流程、資料與檔案安全契約、28 項測試及剩餘限制。

### 使用者操作指南（當前版本）

- 適用範圍：Milestone 4 Editable Modules（包含本紀錄的 stage commit）。
- 前置條件：Windows、.NET 10 SDK；不需 API key、credential 或外部服務。若組織 Windows App Control 封鎖新 build，仍需依既有安全政策核准。
- 啟動：`dotnet run --project .\src\ModuLens.App\ModuLens.App.csproj`。
- 操作：點擊 `Open JavaScript…` 選擇 `.js`；從左側選擇 module，在右側直接修改 content；切換 module 會先將目前 buffer 套用至 in-memory document；按 `Save` 將完整文件寫回。開啟其他檔案或關閉視窗時，如有未儲存內容會要求確認。
- 預期輸出與成功確認：修改後 status 顯示 `Unsaved changes.` 且 Save 可用；成功後顯示 `Saved. N modules detected.`。重新開啟檔案應看見修改，其他 module 與 heading 保持不變。
- 當前限制與安全注意事項：一次只開啟一個檔案，heading 不在 editable buffer；偵測到外部檔案修改時會拒絕儲存，使用者需自行保留 edits 並重新載入／整合；尚無 conflict merge、備份歷史、Git、diff、graph、legacy encoding detection 或 repository workflow。

## 14:18 — Milestone 5 Git Module Status

- 使用者需求摘要：使用者驗收 Milestone 4 編輯狀態無誤，授權接續 roadmap 下一階段 Milestone 5。
- 解讀、假設與設計決策：本階段固定比較目前來源與 `HEAD`，只提供 module-level status，不提前實作 side-by-side diff。Core 以 `(Exact Section Name, 1-based Occurrence Index)` 作為跨版本 identity，ordinal 比較完整 `FullRange`；line range 不參與 identity。Git process 位於 App adapter，使用 `ProcessStartInfo.ArgumentList` 直接呼叫 local `git`，不經 shell。完整決策見 `docs/decisions/0003-project-head-changes-onto-section-identities.md`。
- 實際動作與變更檔案：Core 新增 `IGitService`、`GitFileBaseline`、`SectionIdentity`、`SectionChange`、`SectionComparer`；App 新增 cancellable process runner 與 `LocalGitService`，並抽出共用 source byte decoder；ViewModel 加入 HEAD snapshot、module comparison projection、removed module selection、Git summary 與 edit/Git refresh race protection；module list 改顯示 `unchanged`、`modified`、`added`、`removed` 或 `not compared`；同步 `PRD.md`、`Goal.txt`、development plan、ADR 與本 worklog。
- failure contract：不在 repository 時保留 editor 功能並顯示 `not compared`；repository 尚無 HEAD 或檔案不在 HEAD 時，所有 current modules 標為 `added`；Git executable／command／decode 失敗會清除 badges 並顯示明確 unavailable 訊息。HEAD-only identities 以 `removed` 顯示且無 current source 可編輯；rename 在 V1 表示 removed + added。第一個 heading 前的文字變更另在摘要中標示，避免誤報所有 modules unchanged。
- 測試與驗證：`dotnet test .\ModuLens.slnx --configuration Release --no-restore` 通過 42/42（Core 20、App 22）；涵蓋四種 change kinds、duplicate occurrence、line movement、missing HEAD、not-repository、含空白 path 的 argument vector、BOM HEAD blob、pending edit 與 Git refresh race、removed selection 及 unmapped preamble。`dotnet format ... --verify-no-changes` exit code 0；實際 repository smoke check 的 `rev-parse`、`cat-file -e HEAD:sample.js`、`git show HEAD:sample.js` 均 exit code 0，HEAD blob 為 2,774 行。
- 未完成／受阻與建議下一步：baseline 目前固定 HEAD；沒有 staging/index 狀態、alternate revision、per-line counts 或詳細 diff。重新命名及 duplicate reorder 仍採簡單 identity 規則。下一個未開始階段為 Milestone 6 Module Diff。
- 對使用者的最終回饋摘要：Milestone 5 已完成並以獨立 commit 提交；回報 Git status 顯示、identity／comparison 規則、42 項測試、實際 Git smoke check 與目前限制。

### 使用者操作指南（當前版本）

- 適用範圍：Milestone 5 Git Module Status（包含本紀錄的 stage commit）。
- 前置條件：Windows、.NET 10 SDK；若要比較 HEAD，需安裝 Git 並可從 `PATH` 執行 `git`，來源檔需位於本機 Git work tree。無 Git 或非 repository 檔案仍可正常瀏覽、編輯與儲存。
- 啟動：`dotnet run --project .\src\ModuLens.App\ModuLens.App.csproj`。
- 操作：開啟 repository 內的 `.js`；左側每個 module 右方會顯示 HEAD comparison status，底部右側顯示 baseline 摘要。修改並儲存後會重新讀取 HEAD 並更新 status。選取 `removed` 項目會顯示 `Removed from working tree`，且 current editor 停用。
- 預期輸出與成功確認：未修改的 tracked file 應顯示 `unchanged`；修改一個 module 並儲存後，該項顯示 `modified`；HEAD 不含的新 module 顯示 `added`；只在 HEAD 的 module 顯示 `removed`。非 repository 檔案顯示 `not compared`。
- 當前限制與安全注意事項：只比較 HEAD 與目前完整來源，不顯示逐行內容；不區分 staged／unstaged、不支援選擇 commit／branch、rename detection 或 conflict merge。Git 操作是 read-only，不會 stage、commit、checkout 或修改 repository。
